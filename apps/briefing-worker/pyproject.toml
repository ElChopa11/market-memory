[project]
name = "mm-briefing-worker"
version = "0.1.0"
description = "Market Pulse worker (stub)."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
