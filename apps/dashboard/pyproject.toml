[project]
name = "mm-dashboard"
version = "0.1.0"
description = "Read-only ops UI (stub; later phase)."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
