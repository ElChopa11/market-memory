[project]
name = "mm-execution-service"
version = "0.1.0"
description = "Isolated execution deploy unit (stub). Separate secrets later."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
