[project]
name = "mm-paper"
version = "0.1.0"
description = "Shadow ledger and fill simulation (stub)."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
