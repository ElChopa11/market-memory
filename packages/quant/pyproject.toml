[project]
name = "mm-quant"
version = "0.1.0"
description = "Phase 5c Quant factor library (Tier 4). Research-only; no execution."
requires-python = ">=3.12"
dependencies = [
  "mm-common",
  "pyyaml>=6.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
