"""Factor registry stub. No implementations in Phase 5a (IMP-009)."""


class FactorRegistry:
    """Empty registry so later phases can import a stable name."""

    def names(self) -> tuple[str, ...]:
        return ()
