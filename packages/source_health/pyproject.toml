[project]
name = "mm-source-health"
version = "0.1.0"
description = "Standing data-quality / source-health report (Data & Market Memory Desk)."
requires-python = ">=3.12"
dependencies = [
  "mm-common",
  "mm-memory",
  "mm-ingest",
  "httpx>=0.27",
  "pyyaml>=6.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
