[project]
name = "mm-briefing"
version = "0.1.0"
description = "Market Pulse briefing engine (Phase 3)."
requires-python = ">=3.12"
dependencies = [
  "mm-common",
  "mm-memory",
  "httpx>=0.27",
  "pyyaml>=6.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
