[project]
name = "mm-briefing"
version = "0.1.0"
description = "Market Pulse briefing (stub; Phase 3)."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
