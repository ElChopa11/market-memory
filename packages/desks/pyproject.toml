[project]
name = "mm-desks"
version = "0.1.0"
description = "Phase 5a desk skeletons (Crypto 3a / Equities 3b). No runners, no adapters."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
