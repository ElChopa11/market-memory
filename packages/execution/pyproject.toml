[project]
name = "mm-execution"
version = "0.1.0"
description = "Hyperliquid execution client — live gated (stub)."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
