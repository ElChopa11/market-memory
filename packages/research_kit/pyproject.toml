[project]
name = "mm-research-kit"
version = "0.1.0"
description = "Helpers to write research artifacts. No secrets."
requires-python = ">=3.12"
dependencies = [
  "mm-common",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
