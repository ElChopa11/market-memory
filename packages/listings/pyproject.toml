[project]
name = "mm-listings"
version = "0.1.0"
description = "Phase 6d listings / IPO screen library (Research-owned; no execution)."
requires-python = ">=3.12"
dependencies = [
  "mm-common",
  "mm-provenance",
  "mm-quant",
  "mm-flow",
  "pyyaml>=6.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
