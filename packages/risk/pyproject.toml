[project]
name = "mm-risk"
version = "0.1.0"
description = "Deterministic allow/block risk engine (stub)."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
