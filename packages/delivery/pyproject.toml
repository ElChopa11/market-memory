[project]
name = "mm-delivery"
version = "0.1.0"
description = "Phase 5e Telegram delivery for desk packs. Dry-run default; no execution."
requires-python = ">=3.12"
dependencies = [
  "mm-common",
  "httpx>=0.27",
  "pyyaml>=6.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
