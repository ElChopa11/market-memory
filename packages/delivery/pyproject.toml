[project]
name = "mm-delivery"
version = "0.1.0"
description = "Phase 5d no-send payload strings. Telegram Bot API send is Phase 5e."
requires-python = ">=3.12"
dependencies = [
  "mm-common",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
