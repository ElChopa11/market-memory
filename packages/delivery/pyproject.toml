[project]
name = "mm-delivery"
version = "0.1.0"
description = "Phase 5a delivery skeleton. Telegram send is Phase 5e."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
