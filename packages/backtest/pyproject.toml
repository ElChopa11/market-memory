[project]
name = "mm-backtest"
version = "0.1.0"
description = "Reproducible evaluation harness (event/bar replay, params_hash)."
requires-python = ">=3.12"
dependencies = [
  "mm-common",
  "pyarrow>=17.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
