[project]
name = "mm-backtest"
version = "0.1.0"
description = "Reproducible evaluation harness (stub)."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
