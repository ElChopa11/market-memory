[project]
name = "mm-provenance"
version = "0.1.0"
description = "Truth cell: normalize, dedupe, contradict observations (stub)."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
