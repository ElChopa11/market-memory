[project]
name = "mm-common"
version = "0.1.0"
description = "IDs, time, money, hashing, and shared schemas."
requires-python = ">=3.12"
dependencies = [
  "pydantic>=2.9",
  "httpx>=0.27",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
