# Hyperliquid ingest instruments — Principal lock 2026-09-17.
# Membership source of truth: config/universe.yaml (crypto_perps).
# Default HL info ingest: BTC, ETH, UNI, AAVE (full locked membership).
# Call priority is not equal: BTC/ETH are active_calls; UNI/AAVE are watch-only
# (no thesis priority) per Principal demotion after Skeptic PR #14 / call cards PR #13.
# Must-cuts (HYPE, SOL, XRP, ARB, NEAR, LINK) stay out.
# Equities (NVDA, AVGO, SMH, MSFT, META, JPM, XLF, XOM, IBIT) are a research/briefing watchlist
# for Phase 3 Market Pulse / future equity feeds — not Hyperliquid perps. Do not add them here.
# SMH and IBIT are watch-only in universe.yaml (IBIT = BTC equity proxy, Principal 2026-09-17,
# BTC adjacency to RQ-20260917-A); NVDA/AVGO/MSFT/META/JPM/XLF/XOM are active_calls.
venue: hyperliquid
kind: perpetual
instruments:
  - symbol: BTC
    enabled: true
  - symbol: ETH
    enabled: true
  - symbol: UNI
    enabled: true
  - symbol: AAVE
    enabled: true
